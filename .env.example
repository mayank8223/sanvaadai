# =============================================================================
# T1 – Core infrastructure (see docs/T1-infrastructure-checklist.md)
# Copy this file to .env.local and fill in values. Do not commit .env or .env.local.
# Database, Auth (and optional Storage) are provided by a single Supabase project.
# =============================================================================

# -----------------------------------------------------------------------------
# App
# -----------------------------------------------------------------------------
NEXT_PUBLIC_APP_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Database (T8 – Supabase; T9 – Drizzle ORM for schema/migrations)
# -----------------------------------------------------------------------------
# App uses Supabase client (URL + anon key below) for queries and auth.
# Schema is managed with Drizzle ORM in apps/web (db/schema.ts). Run migrations
# with: cd apps/web && bun run db:migrate (uses DATABASE_URL below).
# Use Connection pooler (port 6543); avoid direct connection (5432) to prevent CONNECT_TIMEOUT.
# If your password contains / or %, URL-encode: / → %2F, % → %25 (e.g. pass/word%1 → pass%2Fword%251).
# Example: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
DATABASE_URL=

# -----------------------------------------------------------------------------
# Auth (T10, T12 – Supabase Auth for web and mobile)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=  # server-only, for admin operations

# -----------------------------------------------------------------------------
# Object storage (media uploads – wire in when implementing uploads)
# -----------------------------------------------------------------------------
# Option A: Use Supabase Storage – same URL/keys as above; create bucket in Supabase dashboard.
# Option B: S3-compatible (e.g. AWS S3, R2):
# S3_BUCKET=
# S3_REGION=
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=

# -----------------------------------------------------------------------------
# LLM (T27 – Vercel AI SDK)
# -----------------------------------------------------------------------------
OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

# -----------------------------------------------------------------------------
# Speech-to-Text (T28 – e.g. Whisper)
# -----------------------------------------------------------------------------
# OPENAI_API_KEY is reused if using Whisper

# -----------------------------------------------------------------------------
# Maps (T25 – submission location preview)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
# NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=
