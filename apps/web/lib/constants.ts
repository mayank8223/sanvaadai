/**
 * Shared constants for the admin web app.
 * Centralizes app name and default metadata for layout and pages.
 */

export const APP_NAME = 'Sanvaadai Admin';

export const DEFAULT_METADATA = {
  title: APP_NAME,
  description: 'Admin interface and API for the data collection platform.',
} as const;
